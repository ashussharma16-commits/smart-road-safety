# URL of the deployed backend API. Set this in Vercel/Netlify project
# settings (or a local .env file) before building for production.
# Local dev falls back to http://localhost:8000 if unset.
VITE_API_URL=https://your-backend.onrender.com
