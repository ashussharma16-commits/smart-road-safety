services:
  - type: web
    name: smart-road-safety-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
    healthCheckPath: /api/health
